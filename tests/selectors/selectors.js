const indexSelectors = require("./homeSelectors")
const registerSelectors = require('./registerSelector')

module.exports = selectors = {
    indexSelectors: indexSelectors,
    registerSelectors: registerSelectors
}